# 2.2.26 — Windows

- 挥鞭冷却缩短至 400ms；保留原用户参数备份。
- 挥鞭灵敏度 60–180% 联动转速、动态加速度、角位移等阈值；以固定基线计算，不累积缩放，保留安全下限。松开滑条后保存/下发。
- 挥鞭卡片改为与双敲识别一致的全宽布局。
- 通用页移除停止监听按钮；新增 Codex/Claude 桌面对话目标选择。切换目标自动关闭实际发送，重新核对窗口和输入框。
- PCB 伤口、鞭子音效独立开关；伤口频率滑条 0–10，0/1 均为每次出现，N>=2 为每 N 次出现。
- Claude 输入框使用 UIA 的 accessible Name 识别，不再把 placeholder/draft rich_text 当成控件标识。找不到唯一输入框或存在草稿时拒绝发送。

## 验证与边界

- 相关单元测试、界面测试和 Windows 打包通过；最终目标适配测试 3 项、界面测试 37 项通过。
- 实际 Claude 桌面新对话的空输入框检查通过，界面显示“已找到且输入框就绪”。未发送任何测试消息，未开启实际发送。
- 已观察到关闭设置后，鞭子显示在 Claude 窗口上。设置打开期间仍按原逻辑隐藏覆盖层。
- 新版运行时开发板参数同步收到确认；本次未改写固件。灵敏度手感、Claude 实际消息提交需用户验证。
- Claude 本次仅适配 Windows；macOS 选择 Claude 会明确拒绝，不会误发到 Codex。

输出：`dist/CodexWhip-2.2.26.exe`。旧版 2.2.25 保留。
