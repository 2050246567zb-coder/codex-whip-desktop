# 产品版 2.2.64

- 修复双敲完全不触发：删除旧版高门槛双敲校准数据，恢复 1.0 g 产品默认值。
- 灵敏度滑条只覆盖 0.5–4.0 g 的可用区间；固件 0.8.1 同时限制最高门槛，旧桌面端也不能再把双敲配置成不可触发状态。
- 语音设置升级为 schema 2，旧的力度学习与输入方式数据不再迁移。
- 修复 Windows 蓝牙刚连接时录音动画瞬间退出：固件等待 MTU 与连接间隔协商完成，并使用最大带宽通知队列持续发送分段 ADPCM；实机连续接收 84,480 个采样后按静音正常结束。
- GitHub Actions 从仓库 Secret 注入豆包 Key；Key 不进入 Git 历史，本地构建可从同名环境变量注入。
