# 产品版 2.2.50 — 敲击硬门槛与深度睡眠动画

- 双敲识别与自动测量加入 0.75 g 物理冲击下限，低于门槛的普通移动不会计数。
- 双敲力度滑条最低值同步为 0.75 g，旧的过低设置在运行时也会受到保护。
- 省电时鞭子平滑变为呼吸浮动的 Z，文字在 `deep sleep.` 至 `deep sleep...` 循环。
- 唤醒、断连以及其他动画状态都从当前帧用非线性曲线平滑过渡。
- Windows 和 macOS 使用相同状态机；macOS 仍需在目标 Mac 上完成真实手柄验收。
