# 产品版 2.2.39 — 双语音输入与跨平台同步

- 设置 > 输入 > 语音输入新增“文字识别 / Codex 原生听写”互斥选择。
- 原有本地与云端文字识别保持不变；双敲根据已保存模式启动对应链路。
- 原生听写把手柄 PCM 实时送到已知虚拟音频设备，Windows 支持 Virtual Audio
  Driver/VB-CABLE，macOS 支持 BlackHole 2ch；绝不自动回退到扬声器。
- Windows UI Automation 与 macOS Accessibility 分别安全启停同一个 Codex
  听写控件；保留启动控件引用，不按名称查找通用“停止”按钮。
- 原生听写完成后，下一鞭提交 Codex 已有草稿，不重复注入文字；空草稿拒绝发送。
- Windows 与 macOS 共享产品功能和配置格式，各自采用原生窗口与音频适配层。

虚拟音频驱动为系统级第三方组件，不随产品版静默安装。云端构建只验证软件包、
测试和启动结构，真实 BLE、虚拟音频与 Codex 听写需在目标设备实测。
