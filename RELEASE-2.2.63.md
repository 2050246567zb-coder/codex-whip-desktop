# 产品版 2.2.63

- 共享固件升级为 `0.8.0`。语音从 Base64 文本改为带序号与 CRC16 的二进制
  ADPCM 分片，按协商 MTU 保持单通知传输。
- 电脑每四个完整分片确认一次；固件最多保留八个未确认分片。队列拥塞、电脑
  确认超时或 MTU 不足只结束当前录音，蓝牙连接保持。
- 电脑仍先完整组装并校验录音，再提交豆包录音文件识别 2.0；旧 `AUD1` 固件
  继续兼容。
- 每次静止三秒完成归中时，同时从低波动窗口更新当前连接的陀螺仪零偏，修复
  新板归中后继续向同一方向漂移。
- Windows 与 macOS 共用协议、姿态算法和固件；平台原生窗口层保持独立。

验证边界：源码测试与本机固件编译不能代替真机 BLE 录音验收。烧录后需实际
双敲、说话，确认录音结束后仍保持连接，并观察静止归中后的持续漂移。
