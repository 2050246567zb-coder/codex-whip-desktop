# 产品版 2.2.61

- 新增隐藏的多手柄自动选择策略，不增加设置页面选项。
- 扫描所有名为 `CodexWhip` 的设备，优先选择 RSSI 最强的近距离设备。
- 记住最近一次成功建立连接的设备；信号差距不超过 6 dB 时保持原设备，避免重连时来回切换。
- 如果原设备明显更远，则自动改连信号更强的设备，并更新记录。
- Windows 和 macOS 使用同一套选择规则，固件保持 0.7.4。
