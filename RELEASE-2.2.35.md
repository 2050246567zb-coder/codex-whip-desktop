# 产品版 2.2.35 — 旧 XIAO 静止归中修复

## 原因与改动

2026-09-20 旧 XIAO（固件 0.6.0）静止采样 1448 帧，姿态算法没有一次触发归中。
末尾 8 秒 800 帧的陀螺仪均值为 (1.829875, -3.611, -0.4235) °/秒，
各轴标准差约 (0.060, 0.062, 0.147) °/秒。旧算法将稳定零偏持续积分为
转动，累计超过 3° 即重置静止计时，所以等不到 3 秒。

- 在接受蓝牙数据之前，按当前设备身份读取独立的静止零偏档案。
- 仅姿态跟随/归中使用该补偿，不更改原始数据、挥鞭/双敲阈值或语音设置。
- 补偿在数据中断重置后保留；连接其他板时清除，未建档设备使用零补偿。
- 自动归中不学习零偏，避免把真实慢转当作零偏消除。
- 归中成功增加诊断日志。没有重刷固件。

本机静止测量档案独立存储于用户数据目录 sensor-bias-profiles.json，不把用户
设备身份或补偿硬编码进 EXE。Windows 蓝牙地址与 macOS UUID 不同，不能直接
把此档案当作跨系统实机验收结果。温漂或更换传感器后应重新静置测量。

## 验证

- 151 项相关测试通过，包括带固定零偏不能归中的复现、补偿后归中、真实慢转
  保留、重连/换板隔离、原始动作数据不变、通知前加载及现有浮层回归测试。
- 相同真实数据回放：修改前 0 次归中，补偿后 4 次，相邻间隔约 3.02 秒。
  最后位置由横向饱和 190 降至约 -0.003、纵向约 -0.009（内部映射单位）。
- Windows EXE 打包成功，旧版保留；修改前源码在 output/pre-idle-fix-2.2.35/。
- 新采样复测首次蓝牙特征发现失败，随后扫描不到设备；用户断电重启后恢复。
  实机新采样 1448 帧，包含转动及随后静置：补偿后触发 3 次归中，未补偿的
  对照算法 0 次。尚未声称用户窗口中的最终观感已经验收。

原始证据：output/xiao-idle-before.json、output/xiao-idle-after.json。
