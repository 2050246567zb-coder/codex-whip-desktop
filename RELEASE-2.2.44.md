# 产品版 2.2.44 — 修复双敲进入录音后界面卡死

- Windows/macOS 的录音开始、识别完成和错误提示音改为后台播放；音频驱动即使阻塞，也不会再占住 Tk 主界面线程。
- 保留 2.2.43 的高频姿态与声纹帧合并，避免录音期间显示事件挤占界面事件泵。
- 新增本机界面挂起诊断：若主线程连续 3 秒没有响应，会把所有 Python 线程栈写入用户数据目录的 `hang-diagnostics.log`，便于下一次复现直接定位，不收集录音或对话内容。
