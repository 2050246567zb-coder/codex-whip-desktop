# 产品版 2.2.45 — 修复原生听写启动卡死

- 根据 2.2.44 的真实挂起线程栈，确认阻塞点是 Windows UI Automation 扫描 Codex 窗口，不是虚拟音频驱动。
- Codex 原生听写的窗口查找、启动和停止全部移到独立线程；即使 UI Automation 提供程序失去响应，软件主界面也不会被锁死。
- Windows 目标发现先用原生 Win32 枚举并按进程过滤，只对实际 Codex/Claude 窗口创建 UIA 包装，不再读取桌面上每个程序的辅助功能可见性。
- 原生听写启动超过 6 秒会安全取消；过期的后台结果不会再次启动手柄录音。
