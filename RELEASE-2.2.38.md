# 产品版 2.2.38 — 手柄省电模式

- 设置 → 手柄顶部新增「省电模式」，沿用现有圆角卡片、开关和字体。
- 默认关闭；电脑保存偏好，连接支持的固件后同步，并显示手柄确认状态。
- 本次支持旧 XIAO nRF52840 Sense / LSM6DS3TR-C，固件 0.6.1。
  ESP32-C3 0.7.x 尚未实现，界面明确提示不生效，不会误下发省电指令。
- 开启后，连续 300 秒没有有效运动进入低功耗待机：陀螺仪关闭，
  加速度计 26 Hz 低功耗，停止 RAW 数据推送，CPU 使用 FreeRTOS 延时，
  蓝牙请求 100 ms 连接间隔（最终间隔由电脑协商），RGB 指示灯关闭。
- 加速度变化 >0.35 g 单次，或相对睡前姿态变化 >0.15 g 连续两次时唤醒。
  可由拿起、晃动、明显倾斜触发；不是空间位移测量，极慢的纯水平旋转
  可能不唤醒。保留蓝牙连接，不使用必须重启/插 USB 的 SYSTEMOFF。
- 活跃时静止判断允许零偏和微抖：20 °/s、0.15 g 方向变化、0.25 g 动态偏差，
  持续 80 ms 才重置计时；大于 80 °/s 或 0.45 g 的明显动作立即重置。
  固定基准避免小变化连续累积被追随而永远判静止，计时支持 millis 回绕。
- 唤醒恢复精确的原传感器寄存器配置，300 ms 稳定期；电脑清旧动作缓存、
  保留安装方向和零偏，500 ms 内不触发挥鞭/双敲，避免拿起误发消息。
- 录音/板端学习不会入睡；电脑端校准期间定期延长清醒计时。
- 开关在手柄内部 Flash 保存，仅变更时写入；电脑未启动时也能待机/唤醒。
  此模式不能代替电池保护，也不能证明之前的 USB 唤醒故障原因。

## 验证

- Windows EXE 和 XIAO 固件编译；板上 POWERTEST 12/12 通过。
- 新增设置持久化、旧固件/C3 隔离、状态切换、清缓存和拾起抑制回归测试。
- 固件已实际烧录，BLE 回读 0.6.1、POWERTEST 12/12。
- 142 项后端相关回归通过，另新增 2 项关闭校准页的回归通过；
  48 项界面/设置测试通过，46 项协议/录音回放/增益测试通过。
- **完整静止五分钟 → 入睡 → 实体移动唤醒尚未验收**：第二轮测试第
  198.55 秒观测到约 223 °/s 明显运动和重力方向变化，390 秒测试窗口内
  没有获得连续五分钟静止。已恢复 POWER,0 并收到确认，未把超时记为通过。
  过程数据见 output/xiao-power-test.json；等待用户确认放稳后再开始完整复测。
- 未测量电池电流、续航或断电后的长期唤醒可靠性，不声称达到微安级深睡。
- 原版固件和本次改动前源码保留在 output/pre-power-saving-2.2.38。

## 设备协议（不涉及向 Codex 发送内容）

- POWER,0 / POWER,1：保存开关并确认 POWER,<0|1>,<ACTIVE|SLEEP>,300。
- POWERGET：查询状态；不唤醒、不延长计时。
- POWERHOLD：暂时唤醒并重置五分钟计时，供校准使用，不保存设置。
- POWERTEST：无硬件副作用的计时/阈值策略自检，返回 POWERTEST,12,12。
- POWERERR：保存或传感器配置错误；恢复失败会低频重试，不发送错误量程数据。

依据：ST LSM6DS3TR-C 数据手册 CTRL1_XL / CTRL2_G / CTRL6_C，
https://www.st.com/resource/en/datasheet/lsm6ds3tr-c.pdf
