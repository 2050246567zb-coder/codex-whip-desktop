# 产品版 2.2.60

- 设置页连接区合并为“手柄 / Codex”单行状态，删除目标应用选择与停止监听按钮。
- 产品固定支持 Codex；旧配置里的 Claude 选择会自动迁移为 Codex。
- 语音识别只保留“豆包录音文件识别 2.0 → 本地 Whisper 备用”。
- 删除旧豆包极速版、OpenAI、百炼、硅基流动及旧语音输入方式的产品入口。
- Windows 与 macOS 构建支持通过 `CODEX_WHIP_DOUBAO_API_KEY` 注入同一个产品 Key。
- 共享固件保持 0.7.4，不需要重新烧录。

本版没有写入真实 API Key；配置仓库 Secret 或本地构建环境变量后，安装包才会优先使用
豆包 2.0。无 Key 时会直接使用本地识别。
