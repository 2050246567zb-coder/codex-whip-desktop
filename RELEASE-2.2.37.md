# 产品版 2.2.37 — 豆包识别与录音增益

- 设置 → 输入 → 识别服务，新增豆包 V3 录音极速识别，支持 API Key 和 App ID + Access Token 两种鉴权。
- 新增 1–8 倍连续录音增益，默认 2 倍，保存后从下一条录音生效。
- 对整段 PCM 使用峰值受限的线性增幅，避免新增削波；极低电平静音保持原样。
- 回放保存实际送去识别的放大后音频，既有录音不追溯修改。
- 不改板端固件、动作检测、归中和用户已有服务选择；不会自动启用豆包或上传旧录音。

165 项相关测试分组通过：请求/响应、业务失败、静音、旧版凭据安全处理、
增益持久化/录音快照/峰值保护/回放一致性、设置 UI，以及姿态/归中/BLE 回归。
未使用真实豆包凭据发起付费请求；用户的权限、额度与实际识别效果尚待验证。

本机上一条录音（只计算幅度、未上传）长约 15.02 秒，峰值 -10.4 dBFS、
全段 RMS -41.2 dBFS；RMS 包含停顿，不等于纯人声响度。默认 2 倍约 +6dB；
更高设置可能受峰值限制，不能保证 8 倍设置始终得到 8 倍实际增幅。

修改前备份：output/pre-doubao-gain-2.2.37/；旧程序保留。
详见 desktop/CLOUD_SPEECH.md，包括官方接口链接及鉴权说明。
