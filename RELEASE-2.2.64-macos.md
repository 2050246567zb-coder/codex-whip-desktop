# Codex Whip 2.2.64 for macOS

合并产品版 2.2.64，并保留 Mac 原生透明鞭子叠层、右键/Control-click 交互、主页动画同步、动作平滑和原生 Whisper 支持。

- 修复双敲门槛过高导致无法触发录音：默认 1.0 g，可调范围 0.5–4.0 g。
- 校准文件采用跨平台 LF 校验，保留 BLE 录音开始、结束和断线诊断日志。
- 语音设置升级到 schema 2；旧设置恢复默认，包括关闭语音开关。升级后请重新开启并检查设置。
- 本次桌面安装包不会刷写手柄固件。
- 支持 Apple Silicon。安装包为 ad-hoc 签名，未做 Apple 公证；更新后可能需要重新授予辅助功能权限。
- 语音 API 需要配置有效 Key；未配置时使用本地 Whisper。此本地构建未内置 API Key。

验证：Mac 单元测试、打包应用 UI 冒烟、应用签名与出厂校准校验；具体结果见发布验证记录。
