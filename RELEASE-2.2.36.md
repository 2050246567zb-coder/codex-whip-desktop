# 产品版 2.2.36 — 上次识别录音回放

包含 2.2.35 旧 XIAO 独立静止零偏与三秒归中修复，详见 RELEASE-2.2.35.md。

## 使用

设置 → 输入 → 识别服务 → 播放上次录音。播放中同一按钮变为“停止播放”。
暂无录音时禁用；生成第一条完整识别录音后自动可用，无需重新打开设置。
显示音频时长，只在本机保存最近一次，重启后仍可听。

- 回放的是完整接收并送入转写器的 16kHz 单声道 PCM16 音频，不是文字合成音。
- 空识别结果及云端/本地识别异常均保留该次音频，方便判断收音问题。
- 丢包、取消、TX_FAILED、过短录音没有进入转写，不覆盖上一条完整识别录音。
- 新录音原子替换最近文件，存储失败不阻断正常转写。
- 本地文件 last-voice-recording.wav 位于 voice-settings.json 同目录。
- 播放不重新调用识别、不上传、不发送消息；关闭设置销毁卡片及开始录音时停止回放。
- Windows 使用独立 MCI 音频实例，避免停止回放时关闭鞭打音效；macOS 提供 afplay 路径，尚未实机验证。

## 验证

相关模块合计 201 项测试通过（Tk 测试在单进程批量运行时出现 Tcl 路径初始化
错误，受影响的设置/浮层测试分别在独立进程重跑通过）。覆盖原始 PCM 一致性、
空结果/识别失败保留、无效录音不覆盖、磁盘失败继续识别、独立播放快照、
按钮禁用/播放/停止/录音中禁用/销毁清理，以及归中和浮层回归。

Windows 真实播放接口使用静音 WAV 测试：playing=True，停止后 playing=False。
这不等于真实麦克风收音质量验收；需要用户触发一次新录音后试听。

旧版程序及用户校准数据保留，不改开发板固件。
