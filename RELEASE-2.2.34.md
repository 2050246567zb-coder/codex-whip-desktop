# 产品版 2.2.34

2026-09-20：实体手柄跟随范围扩大为目标应用的整个实时窗口。

- 取消默认的中间三分之一范围，使用相同姿态角度覆盖整个窗口。
- 每个动画帧读取当前窗口尺寸；移动、缩放、最大化、全屏及恢复窗口后，不需要重新连接或校准。
- 手柄定位点限制在窗口内；鞭绳动画仍可越过窗口边缘。
- 屏幕运动方向判定与显示位置共用映射，保留现有平滑处理、自动归中、识别阈值及用户数据。
- 不改开发板固件，继续使用 0.7.5。

验证：175 项相关自动化测试通过。新增同一浮层在普通窗口、最大化、全屏、负坐标副屏及恢复窗口间变化的测试，覆盖中心、四向范围、越界限幅与运动方向坐标一致性。真实窗口切换及手持跟随观感尚待用户验证。

旧版 dist/产品版-2.2.33.exe 保留；本次修改前源文件备份在 output/pre-full-window-2.2.34/。新包 dist/产品版-2.2.34.exe，不要与旧版同时运行。
